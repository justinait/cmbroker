const config = {
    EMAIL:{
        service_id: import.meta.env.VITE_API_SERVICEID,
        template_id: import.meta.env.VITE_API_TEMPLATEID,
        public_id: import.meta.env.VITE_API_PUBLICID
    }
}

export default config;